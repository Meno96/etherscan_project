# La chiave segreta di Django
# SECRET_KEY='your-secret-key'

# Imposta il database in locale, solitamente PostgreSQL per sviluppo
DB_NAME='etherscan_db'
DB_USER='postgres'
DB_PASSWORD='admin'
DB_HOST='localhost'
DB_PORT='5432'

# API Key per Etherscan - necessario per interagire con l'API di Etherscan
ETHERSCAN_API_KEY='your-etherscan-api-key'

# Debug mode - attivare solo in sviluppo
DEBUG=True